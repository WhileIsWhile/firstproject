{{>layouts/header}}

<table class="table">
    <thead>
    <tr>
        <th scope="col">ID</th>
        <th scope="col">TITLE</th>
        <th scope="col">CONTENT</th>
    </tr>
    </thead>
    <tbody>
        {{#article}}
            <tr>
                <th>{{id}}</th>
                <td>{{title}}</td>
                <td>{{content}}</td>
            </tr>
        {{/article}}
    </tbody>
</table>
<!--모델 범위 밖에 서 모델안에 id 값 불러올려면!! article.id 이렇게 하면됨! ㅇ..-->
<a href="/articles/{{article.id}}/edit" class="btn btn-primary">Edit</a>

<!--POST    - 생성
    GET     - 조회
    UPDATE  - 수정
    DELETE  - 삭제  -->
<!--GET , 즉 a태그 링크 사용   , 나중에 JS 사용해서 이동 할꺼임-->
<a href="/articles/{{article.id}}/delete" class="btn btn-danger">Delete</a>
<a href="/articles" class="btn btn-primary">Go to articleList</a>
{{>comments/_comments}}
{{>layouts/footer}}